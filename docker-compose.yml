version: '3.8'

services:
  app:
    build: .
    container_name: dailytracker-app
    restart: unless-stopped
    ports:
      - "8080:8080"
    environment:
      PORT: 8080
      DB_PATH: /app/data/dailytracker.db
    volumes:
      - ./data:/app/data

volumes:
  data:
